import config from '../configinfo'
import React from 'react'

let socket = config.socket;

export default class HelpRequests extends React.Component{
  render(){
    return (
      <div> This is HelpRequests.</div>
    )
  }
}
